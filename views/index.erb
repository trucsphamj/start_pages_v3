<!DOCTYPE html>
<html>
  <head>
    <title>Start Page!</title>
    <link rel="stylesheet" href="css/row.css" type="text/css" />
</head>
<% if $newBg=="default" %>
  <body>
<% else %>
  <body background="#<%=$newBg%>">
<% end %>

    <div class="top_right_text">
          <b>Click here to </b>
          <a href="/customize">customize this page!</a> 
         <%if(session[:email]) != nil%>
               <a href="/logout">  Log out</a>
          <%end%>
    </div>

    <div id="container">
	<ul class="horizontal_list">
		<% #if no custom sites have been set, display the default sites..
		if @sitesHash.empty? %>
			<li><a href="http://www.facebook.com"><img src="images/icon-facebook.png" /></a>
			</li>
			<li><a href="http://www.gmail.com"><img src="images/icon-gmail.png" /></a>
			</li>
			<li><a href="http://www.amazon.com"><img src="images/icon-amazon.png" /></a>
			</li>
			<li><a href="http://www.twitter.com"><img src="images/icon-twitter.png" /></a>
			</li>
			<li><a href="http://www.github.com"><img src="images/icon-github.png" /></a>
			</li>
		<% #if there are custom sites to display, display them:
		else %>
			<%@sitesHash.each do |k, v|%>
				<% @url=@sitesHash.index(v) %>
        			<li><a href="<%=@url%>" id="link">
       				<% @image = @suggestedLinks.select {|k,v| k=="#{@url}"}
					   @image.each do |k, v| %>
						<img src="images/<%= @suggestedLinks[k][1] %>" /></a>
       	 			<%end%>
				</li>
			<%end%>
		<%end%>
	</ul>
    </div>
  </body>
</html>
